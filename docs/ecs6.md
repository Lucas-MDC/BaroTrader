This project uses JavaScript in agreement with ECMAScript6. To this, we add to the project the VSCode extension [ESLint](https://eslint.org/) with the your recommendation:

```bash
$ npm install eslint --save-dev
```

```bash
$ npm init @eslint/config@latest
```

and change the settings project `"type": "module"` in package.json